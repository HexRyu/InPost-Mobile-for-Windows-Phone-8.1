<Page
    x:Class="InPost_Mobile.Views.DetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InPost_Mobile.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#1F1F1F">

    <Grid>
        <ScrollViewer>
            <Border Padding="20">
                <StackPanel>

                    <!-- STANDARD PARCEL DETAILS -->
                    <Border Background="#2D2D2D" Padding="15" Margin="0,0,0,20" Visibility="{Binding IsNotReturnVisibility}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Status}" FontSize="24" FontWeight="Bold" Foreground="White" TextWrapping="Wrap"/>
                                <Border Background="#3D3D3D" Height="1" Margin="0,15,0,15"/>

                                <StackPanel Visibility="{Binding DetailsSenderVisibility}">
                                    <TextBlock x:Uid="Lbl_Sender" Text="Nadawca" FontSize="15" Foreground="#AAAAAA" Opacity="0.7"/>
                                    <TextBlock Text="{Binding Sender}" FontSize="22" FontWeight="SemiBold" Foreground="#DDDDDD" Margin="0,5,0,15" HorizontalAlignment="Left" TextAlignment="Left"/>
                                </StackPanel>

                                <!-- Tracking Number Section -->
                                <TextBlock x:Uid="Lbl_TrackingNum" Text="Numer przesyłki" FontSize="15" Foreground="#AAAAAA" Opacity="0.7" Margin="0,5,0,0"/>
                                <TextBox Text="{Binding TrackingNumber}" IsReadOnly="True" Background="Transparent" BorderThickness="0" 
                                         FontSize="22" FontWeight="SemiBold" Foreground="#DDDDDD" Margin="0,5,0,15" Padding="0" HorizontalAlignment="Left" TextAlignment="Left">
                                    <TextBox.Resources>
                                        <SolidColorBrush x:Key="PhoneTextBoxBrush" Color="Transparent"/>
                                        <SolidColorBrush x:Key="PhoneTextBoxEditBackgroundBrush" Color="Transparent"/>
                                        <SolidColorBrush x:Key="PhoneTextBoxReadOnlyBrush" Color="Transparent"/>
                                        <SolidColorBrush x:Key="PhoneTextBoxForegroundBrush" Color="#DDDDDD"/>
                                        <SolidColorBrush x:Key="PhoneTextBoxEditBorderBrush" Color="Transparent"/>
                                        <SolidColorBrush x:Key="PhoneTextBoxReadOnlyBorderBrush" Color="Transparent"/>
                                    </TextBox.Resources>
                                </TextBox>

                                <!-- Name Section -->
                                <TextBlock x:Uid="Lbl_NameHeader" Text="NAZWA PRZESYŁKI" FontSize="15" Foreground="#AAAAAA" Opacity="0.7" Margin="0,5,0,0"/>
                                <Button x:Name="BtnRename" Click="Rename_Click" HorizontalAlignment="Left" Background="Transparent" Padding="0" BorderThickness="0" Margin="-2,-8,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NameButtonText}" Foreground="{Binding NameButtonColor}" FontSize="19" FontWeight="SemiBold" Margin="0,0,10,0"/>
                                        <TextBlock Text="&#xE104;" FontFamily="Segoe UI Symbol" Foreground="White" FontSize="18" VerticalAlignment="Center" Opacity="0.7"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>

                            <StackPanel Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Right">
                                <Border BorderBrush="White" BorderThickness="2" Width="50" Height="50">
                                    <Grid>
                                        <TextBlock Text="{Binding Icon}" FontFamily="Segoe UI Symbol" FontSize="24" Foreground="White" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsFontIcon}"/>
                                        <Image Source="{Binding IconImage}" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsBitmapIcon}" Margin="5"/>
                                    </Grid>
                                </Border>
                                <TextBlock x:Uid="LblSize" Text="Rozmiar" FontSize="12" Foreground="#AAAAAA" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                <TextBlock Text="{Binding SizeName}" FontSize="16" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- RETURN PARCEL DETAILS -->
                    <StackPanel Visibility="{Binding IsReturnVisibility}">
                        <!-- Card 1: Main Return Info -->
                        <Border Background="#2D2D2D" Padding="15" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="{Binding Status}" FontSize="24" FontWeight="Bold" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,20"/>
                                
                                <Grid Margin="0,0,0,15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock x:Uid="Lbl_ReturnTo" Text="ZWROT DO" FontSize="11" Foreground="#AAAAAA" Opacity="0.8"/>
                                        <TextBlock Text="{Binding SenderDisplay}" FontSize="15" Foreground="#EEEEEE" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                        <TextBlock x:Uid="LblSizeText" Text="ROZMIAR" FontSize="11" Foreground="#AAAAAA" Opacity="0.8" HorizontalAlignment="Right"/>
                                        <TextBlock Text="{Binding SizeName}" FontSize="15" Foreground="White" HorizontalAlignment="Right"/>
                                    </StackPanel>
                                </Grid>

                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock x:Uid="Lbl_TrackingNum" Text="NR PRZESYŁKI" FontSize="11" Foreground="#AAAAAA" Opacity="0.8"/>
                                    <TextBlock Text="{Binding TrackingNumber}" FontSize="15" Foreground="#EEEEEE"/>
                                    
                                    <TextBlock x:Uid="Lbl_NameHeader_Return" Text="NAZWA ZWROTU" FontSize="11" Foreground="#AAAAAA" Opacity="0.8" Margin="0,15,0,0"/>
                                    <Button x:Name="BtnRenameReturn" Click="Rename_Click" HorizontalAlignment="Left" Background="Transparent" Padding="0" BorderThickness="0" Margin="-2,0,0,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding NameButtonText}" Foreground="{Binding NameButtonColor}" FontSize="19" FontWeight="SemiBold" Margin="0,0,10,0"/>
                                            <TextBlock Text="&#xE104;" FontFamily="Segoe UI Symbol" Foreground="White" FontSize="16" VerticalAlignment="Center" Opacity="0.7"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>

                                <StackPanel Margin="0,0,0,0">
                                    <TextBlock x:Uid="Lbl_ReturnDate" Text="ZWROT NADANO" FontSize="11" Foreground="#AAAAAA" Opacity="0.8"/>
                                    <TextBlock Text="{Binding CreationDateText}" FontSize="15" Foreground="#EEEEEE"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Card 2: Size Info -->
                        <Border Background="#2D2D2D" Padding="15" Margin="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock x:Uid="Lbl_ParcelSizeHeader" Text="ROZMIAR PRZESYŁKI" FontSize="11" Foreground="#AAAAAA" Opacity="0.8"/>
                                    <TextBlock Text="{Binding SizeName}" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,2,0,2"/>
                                    <TextBlock Text="{Binding SizeDescription}" FontSize="14" Foreground="#CCCCCC"/>
                                </StackPanel>
                                <Border Grid.Column="1" BorderBrush="White" BorderThickness="2" Width="40" Height="40" HorizontalAlignment="Right" VerticalAlignment="Center">
                                   <!-- The generic box icon in segoe UI symbol is E18B -->
                                   <TextBlock Text="&#xE18B;" FontFamily="Segoe UI Symbol" FontSize="20" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Border>
                        
                        <!-- Card 3: Details (optional) -->
                        <Border Background="#2D2D2D" Padding="15" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock x:Uid="Lbl_DetailsHeader" Text="ZWROT UTWORZONO" FontSize="11" Foreground="#AAAAAA" Opacity="0.8"/>
                                <TextBlock Text="{Binding CreationDateText}" FontSize="15" Foreground="#EEEEEE"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Visibility="{Binding PickupSectionVisibility}" Margin="0,0,0,20">
                        <TextBlock x:Uid="Lbl_PickupPoint" Text="MIEJSCE ODBIORU" FontSize="15" FontWeight="Bold" Foreground="#888888" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding PickupPointName}" FontSize="28" FontWeight="Bold" Foreground="White" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding PickupPointAddress}" FontSize="16" Foreground="#DDDDDD"/>
                    </StackPanel>

                    <StackPanel x:Name="CodeSection" Margin="0,10,0,25" HorizontalAlignment="Center" Visibility="Collapsed">

                        <TextBlock x:Uid="Lbl_PickupCode" Text="Kod odbioru:" FontSize="14" Foreground="#AAAAAA" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding PickupCode}" FontSize="36" FontWeight="Bold" Foreground="#FFCC00" HorizontalAlignment="Center" Margin="0,0,0,15" CharacterSpacing="50"/>

                        <Border Background="White" Padding="15" CornerRadius="12" HorizontalAlignment="Center" Margin="0,0,0,20">
                            <Image x:Name="QrImage" Width="220" Height="220"/>
                        </Border>

                        <Button x:Uid="BtnOpenRemote" Content="Otwórz skrytkę zdalnie" 
                                Click="OpenRemote_Click"
                                Background="#FFCC00" Foreground="Black" FontWeight="Bold" FontSize="20"
                                HorizontalAlignment="Stretch" Height="60" Margin="10,0,10,0"/>
                    </StackPanel>

                    <TextBlock x:Uid="Lbl_HistoryHeader" Text="Historia przesyłki" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,10,0,15"/>

                    <ListView ItemsSource="{Binding History}" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Rectangle Width="2" Fill="{Binding BrushColor}" HorizontalAlignment="Center" VerticalAlignment="Stretch" Grid.Column="0" Opacity="0.5"/>
                                    <Ellipse Width="12" Height="12" Fill="{Binding BrushColor}" VerticalAlignment="Top" HorizontalAlignment="Center" Grid.Column="0" Margin="0,6,0,0"/>

                                    <StackPanel Grid.Column="1" Margin="10,0,0,0" Opacity="{Binding Opacity}">
                                        <TextBlock Text="{Binding Description}" FontSize="16" FontWeight="Bold" Foreground="White"/>
                                        <TextBlock Text="{Binding Date}" FontSize="14" Foreground="Gray"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar Background="#1F1F1F" Foreground="White">
            <AppBarButton x:Name="BtnRestore" x:Uid="Btn_Restore" Icon="Undo" Label="Przywróć" Click="Restore_Click" Visibility="Collapsed"/>
            <AppBarButton x:Name="BtnArchive" x:Uid="Btn_Archive" Icon="Download" Label="Archiwizuj" Click="Archive_Click"/>
            <AppBarButton Icon="Delete" x:Uid="Btn_Delete" Label="Usuń" Click="Delete_Click"/>

        </CommandBar>
    </Page.BottomAppBar>

</Page>